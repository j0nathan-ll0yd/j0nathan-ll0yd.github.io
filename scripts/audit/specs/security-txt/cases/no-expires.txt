This security.txt body has no Expires field at all, and deliberately no
Contact field either -- check-security-txt.mjs:19-23 returns immediately after
emitting security-txt-expires-missing, before the Contact check at :47 ever
runs, so this input must emit ONLY security-txt-expires-missing.
