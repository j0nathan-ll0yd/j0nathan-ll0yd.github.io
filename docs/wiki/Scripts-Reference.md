# Scripts Reference

Every `package.json` script, what it invokes, and which caller owns it.

## Reference Table

| Script                 | What it does                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Used by                                                                                                                                                              |
| ---------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `prepare`              | Runs `husky` to install `.husky/pre-commit`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | npm lifecycle — automatic on `npm install` / `npm ci`.                                                                                                               |
| `check:contract-lock`  | Runs `scripts/check-contract-lock.mjs` — recomputes the expected `.contract-lock.json` from the installed `@j0nathan-ll0yd/schemas` + `@j0nathan-ll0yd/portal-contract` packages and fails on any hand-edit or upstream schema drift.                                                                                                                                                                                                                                                                                                                                                        | `.husky/pre-commit` (Tier 1 — blocks every local commit); `visual-tests.yml` `contract-check` job (Tier 2 — blocks PR merge).                                        |
| `dev`                  | `astro dev` — Astro dev server on `localhost:4321` with HMR and a CloudFront proxy at `/api/live`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Developer (local coding).                                                                                                                                            |
| `generate:webmcp`      | Runs `scripts/generate-webmcp.mjs` — writes `public/js/webmcp.js`, `public/.well-known/mcp/server-card.json`, and `public/.well-known/agent-skills/index.json` from `@j0nathan-ll0yd/portal-contract` constants. Output is byte-stable; edit the generator, not the outputs.                                                                                                                                                                                                                                                                                                                 | `prebuild` (automatic before every `build`); developer manually after a portal-contract version bump.                                                                |
| `prebuild`             | npm lifecycle hook — runs automatically before `build`. Four steps: `generate:webmcp` → DS schemas self-validation (`@j0nathan-ll0yd/schemas/scripts/validate.ts`) → `audit:fixtures` → `audit:inline-scripts`. Any failure halts the build.                                                                                                                                                                                                                                                                                                                                                 | npm lifecycle — automatic before `npm run build`. Never run directly.                                                                                                |
| `audit:fixtures`       | Runs `scripts/audit-fixtures.mjs` — Invariant I2 gate: fails the build if any consumer-side fixture JSON appears under `data/`, `test/fixtures/`, or `src/**/fixtures/`. Fixtures are DS-owned (`@j0nathan-ll0yd/fixtures`).                                                                                                                                                                                                                                                                                                                                                                 | `prebuild` (automatic); developer manually when verifying no local fixtures crept in.                                                                                |
| `audit:inline-scripts` | Runs `scripts/audit-inline-scripts.mjs` — CSP regression gate: rejects `<script is:inline>` bodies and `on*=` event-handler attributes in `src/**` markup, and scans `public/js/` for handler-attribute strings emitted via innerHTML.                                                                                                                                                                                                                                                                                                                                                       | `prebuild` (automatic); developer manually after adding any script wiring.                                                                                           |
| `build`                | `astro build` — compiles `src/` into `dist/`; the SSR shell comes from `@j0nathan-ll0yd/fixtures` via `loadDashboardData()`. Preceded by `prebuild`, followed by `postbuild`.                                                                                                                                                                                                                                                                                                                                                                                                                | `deploy.yml` (`npm run build` step); `playwright.config.ts` `webServer.command`; `vitest.build.config.ts` global setup.                                              |
| `postbuild`            | npm lifecycle hook — runs `scripts/check-live-data-bundle.mjs` to verify Rollup did not tree-shake the side-effect-only `import '@j0nathan-ll0yd/web/runtime/live-data'`. Fails the build if the bundle is absent.                                                                                                                                                                                                                                                                                                                                                                           | npm lifecycle — automatic after `npm run build`. Never run directly.                                                                                                 |
| `preview`              | `astro preview` — serves the built `dist/` locally.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Developer (sanity-check before commit); `playwright.config.ts` `webServer.command` (after `build`).                                                                  |
| `fetch:images`         | Runs `scripts/fetch-images.mjs` — downloads CloudFront-optimized book covers and theatre posters to `public/images/`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Developer manually when CI files a missing-images issue; `deploy.yml` `check-images` job (with `--check-only`).                                                      |
| `test`                 | Alias for `test:build`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | Developer (quick sanity check before push).                                                                                                                          |
| `test:build`           | Runs Vitest with `vitest.build.config.ts` — global setup executes `npm run build` once, then specs in `tests/build/**` assert SEO meta, JSON-LD, image integrity, and service-worker output against `dist/`.                                                                                                                                                                                                                                                                                                                                                                                 | Developer after changing `src/lib/`, `src/layouts/`, or any SEO/JSON-LD code.                                                                                        |
| `test:visual`          | Runs `scripts/run-in-docker.sh playwright.config.ts` inside `mcr.microsoft.com/playwright:v{VERSION}-noble`, **arm64-native** under Docker Desktop's Apple Virtualization Framework. Builds the DS-fixtures SSR shell, intercepts CloudFront to serve raw fixtures from `@j0nathan-ll0yd/fixtures/generated/`, screenshots at 4 viewports, pixel-compares against committed baselines. **Canonical local run** — the self-hosted CI runner runs `npx playwright test --shard=...` inline in the same base image on `[self-hosted, linux, arm64, playwright]`, so PNG bytes match CI exactly. | Developer before any push (enforced by `.husky/pre-push`); CI (`visual-tests.yml` runs tests inline on the self-hosted arm64 runner).                                |
| `test:visual:update`   | Same as `test:visual` but passes `--update-snapshots`. The **only sanctioned path** to regenerate PNG baselines after intentional visual changes; commit the resulting PNGs. Equivalent CI path: dispatch `visual-tests.yml` with `update_snapshots=true`.                                                                                                                                                                                                                                                                                                                                   | Developer after intentional visual changes; `visual-tests.yml` `update_snapshots=true` mode.                                                                         |
| `test:smoke`           | Runs `npx playwright test --config=playwright.smoke.config.ts` against the live `https://jonathanlloyd.me`. Asserts hydration completed: skeletons cleared, bio terminal typed, service worker registered, no CSP or console errors. No baselines, no Docker.                                                                                                                                                                                                                                                                                                                                | `smoke-check.yml` CI workflow (triggered by `workflow_run` after a successful `deploy.yml`); developer after a deploy or when investigating a `smoke-failure` issue. |

## Notes

- **`prebuild` / `postbuild`** are npm lifecycle hooks — they run automatically around `npm run build` and should never be invoked directly.
- **Baseline regeneration** must go through `test:visual:update` (local, arm64-native Docker) or `visual-tests.yml` with `update_snapshots=true` (CI, self-hosted arm64). Both paths use the same Playwright noble base image, so their bytes are byte-identical. Native macOS runs are not sanctioned — macOS-rendered pixels cannot match Linux baselines.
- **`check:contract-lock`** is the single contract gate, enforced in two tiers: Husky pre-commit (local) and the `visual-tests.yml` `contract-check` CI job (PR gate). It recomputes the expected lock from the installed `@j0nathan-ll0yd/schemas` + `@j0nathan-ll0yd/portal-contract` packages and compares every checksum, so it catches both upstream schema drift and hand-edits to `.contract-lock.json`.
- **`fetch:images`** syncs CloudFront-optimized images; CI runs it with `--check-only` and files a GitHub issue rather than failing the deploy if images are missing.
